apiVersion: v1
kind: Service
metadata:
  name: {{ include "devops-app-chart.fullname" . }}
  labels:
    {{- include "devops-app-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "devops-app-chart.selectorLabels" . | nindent 4 }}
